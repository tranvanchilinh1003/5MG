/**
 * Shim Lucide — trang hiện không dùng data-lucide; giữ API để header.js không lỗi.
 * Khi cần icon thật: thay bằng bản build lucide (npm run build:icon) hoặc CDN.
 */
(function (g) {
  g.lucide = g.lucide || {};
  g.lucide.createIcons = function () {};
})(typeof globalThis !== "undefined" ? globalThis : window);
